import os
from building import * 

# get current dir path
cwd = GetCurrentDir()
inc = [cwd]
src = Glob('*.c')

# add group to IDE project
group = DefineGroup('Commons', src, depend = [], CPPPATH = inc)

Return('group')